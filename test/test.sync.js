var assert = chai.assert;


describe('Records synchronization', function() {

    it("single model");
    it("multiple models");
    it("create all missing");
    it("update only if different");
    it("delete remote if not local (push)");
    it("fetch remote if not local (pull)");

});